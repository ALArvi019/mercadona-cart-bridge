[pytest]
asyncio_mode = auto
# Los tests del núcleo (tests/) no necesitan Home Assistant y corren con .venv.
# Los de la integración (tests/ha/) requieren .venv-ha, que lo trae instalado.
testpaths = tests
norecursedirs = tests/ha
